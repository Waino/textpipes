[exp]
n_lines = 1000

[paths.dirs]
tmp = tmp
inputs = inputs
stabile = out_gen_stabile
instabile = out_gen_instabile

[paths.inputs]
count_tokens = ${paths.dirs:inputs}/count
count_tokens2 = ${paths.dirs:inputs}/count2
count_chars = ${paths.dirs:inputs}/count

[paths.outputs]
count_tokens = ${paths.dirs:stabile}/count_tokens
count_tokens2 = ${paths.dirs:stabile}/count_tokens2
count_tokens2_words = ${paths.dirs:stabile}/count_tokens2_words
count_chars = ${paths.dirs:stabile}/count_chars
remove_counts = ${paths.dirs:stabile}/remove_counts
scale_counts = ${paths.dirs:stabile}/scale_counts
combine_counts = ${paths.dirs:stabile}/combine_counts
combine_counts_balance = ${paths.dirs:stabile}/combine_counts_balance
combine_wordlists = ${paths.dirs:stabile}/combine_wordlists
